# secrets — never commit
.env

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# agent runtime output
audit.jsonl
labs/lab3-guardrails/audit.jsonl

# eval output (the committed replay scorecard is scorecard.json — keep that)
labs/lab4-evals/runs/

# generated kubeconfig for the scoped agent ServiceAccount
cluster/rbac/agent.kubeconfig

# macOS
.DS_Store

# Belt and braces. These internal documents live one directory up, outside this
# repository, so they cannot be committed by accident. The rules stay anyway, so
# that dropping a copy in here to refer to while working does not publish it.
build-spec.md
*Syllabus*.pdf
NOTES.md
session-day/
